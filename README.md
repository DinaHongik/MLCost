# MLCost
MLCost: pre-run prediction of training time and energy/cost from MLPerf Training logs. Reproducible pipeline (log extraction → preprocessing → 420-D features → RF/GB/XGB ensemble), configs, and sample data for the paper.
